name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Display repository info
        run: |
          echo "GitHub Pages Project"
          echo "Profile Website"

      - name: List files
        run: ls -R
